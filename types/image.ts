export interface MainImage {
    alt: string;
    asset: {
        _id: string;
        url: string;
    };
}

export interface ImageGallery {
    alt: string;
    asset: {
        _id: string;
        url: string;
    };
}
